import {styled} from '../../config/stitches';

const LoadDogs = styled('div', {
  minHeight: '200px',
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',

  Button: {
    width: '200px',
  },
});

export {LoadDogs};
